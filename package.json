{
  "name": "all-india-pincode",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack MERN application for searching, filtering, visualizing, and exporting India Post PIN code data.",
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c cyan,green \"npm run frontend:dev\" \"npm run backend:dev\"",
    "frontend:dev": "npm run dev --prefix frontend",
    "backend:dev": "npm run dev --prefix backend",
    "frontend:build": "npm run build --prefix frontend",
    "backend:start": "npm start --prefix backend",
    "start": "npm run backend:start",
    "build": "npm run frontend:build",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
